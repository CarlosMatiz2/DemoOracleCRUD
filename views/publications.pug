html
    head
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css")
    style.
      body {
        background: #007bff;
        background: linear-gradient(to right, #0062E6, #33AEFF);
      }
      a{
        text-decoration: none;
        color: #000;
        width: 100%;
      }   
      .card-body{
        display: flex;
        flex-direction: column;
        align-content: center;
      }  
      .card-body .form-group{
        margin-bottom: 10px;
      }
      #createPublication{
        width: 80px;
      }
    script.
        let delete = document.getElementById("delete")
    body 
        div(class="container")
            div(class="row")
                div(class="col-sm-9 col-md-7 col-lg-5 mx-auto w-100")
                    div(class="card border-0 shadow rounded-3 my-5")
                        div(class="card-body p-4 p-sm-5")
                            a(href='/' id="back") Home
                            br
                            a(class="btn btn-primary" id="createPublication" href='/publications/createPublication') Crear
                            br
                            table(class="table table-striped")
                                thead
                                    tr 
                                        th(scope="col") Nombre
                                        th(scope="col") Descripción
                                        th(scope="col") Eliminar
                                        th(scope="col") Editar
                                tbody#tablePublicactionsBody

    script(src='/static/js/getAllPublications.js')